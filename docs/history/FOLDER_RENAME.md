# 项目文件夹重命名完成

## 重命名操作

项目文件夹已成功重命名：

```
/z/cFS/lite-cfs-framework  →  /z/cFS/cspd-bsp
```

## 新的项目路径

```
/z/cFS/cspd-bsp/
```

## 使用新路径

### 进入项目目录
```bash
cd /z/cFS/cspd-bsp
```

### 编译
```bash
cd /z/cFS/cspd-bsp
./build.sh
```

### 运行
```bash
sudo /z/cFS/cspd-bsp/build/bin/cspd-bsp
```

## 完整的项目标识

- **项目名称**: CSPD-BSP
- **项目路径**: `/z/cFS/cspd-bsp/`
- **可执行文件**: `cspd-bsp`
- **日志文件**: `/var/log/cspd-bsp.log`
- **缓存文件**: `/var/cache/cspd-bsp.cache`

## 验证清单

- [x] 项目文件夹名称
- [x] CMake项目名称
- [x] 可执行文件名
- [x] 所有文档
- [x] 源代码注释
- [x] 配置文件
- [x] 日志路径
- [x] 缓存路径
- [x] 术语统一

**所有重命名工作已完成！项目现在完全使用 CSPD-BSP 作为统一标识。**
